#!/usr/bin/env ruby

$LOAD_PATH << File.expand_path('../lib/', __dir__)

require 'thor'
require 'ledger'

begin
  require 'pry'
rescue LoadError
  nil # Ignore when running outside development
end

Ledger::Cli.start
